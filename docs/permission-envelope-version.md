# Permission envelope versioning

The first public explicit-run permission envelope is version 1. It is intentionally limited to a dedicated Codex Session and the `workspace-read`, `workspace-write`, and `network` capabilities. Any future expansion to browser, arbitrary roots, scheduled provisioning, or cross-Host authority requires a new reviewed contract rather than silently widening version 1.
