# Permission approval contents

A sensitive explicit run approval must identify the workflow name, approved goal, request ID, input digest, dedicated working directory, requested capabilities, generated sandbox mode, network flag, writable roots, model/reasoning request, bound Skills, stage count, and envelope digest. Approval text must not hide these fields behind a generic “continue” prompt.
