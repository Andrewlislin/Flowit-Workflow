# Permission envelope invariants

```text
requested capabilities are not authorization
accepted elicitation is scoped to one normalized request
one requestId cannot change its permission envelope
one grant cannot cross working directories
one dedicated Session cannot broaden its approved sandbox
one Pipeline turn cannot silently change the sandbox
unattended approval escalation fails closed
```
