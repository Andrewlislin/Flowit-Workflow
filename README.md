# Flowit Workflow for DeepSeek Harness

Flowit Workflow is a DeepSeek Harness plugin bundle for cross-session orchestration, durable scheduling, Skill binding, and session-context flow.

> Repository initialization in progress. The implementation lives in `src/` and is designed against the DeepSeek Harness `0.1.1-rc.2` plugin seams.
