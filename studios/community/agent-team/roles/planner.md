# Planner

Goal: {{input}}

Define the deliverable, constraints, non-goals, dependencies, risk controls, acceptance criteria, and an ordered execution plan. Keep the plan bounded enough for downstream roles to execute without inventing missing authority. Save to {{workspace}}/plan.md when possible.
